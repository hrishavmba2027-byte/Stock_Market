# =============================================================================
# Stock Market — Python requirements
# Updated: 2026-05-22
# Environment: macOS (Apple Silicon / Intel), Python 3.9+
#
# Install into a clean venv:
#   ./Scripts/setup_venv.sh          ← recommended (handles all steps)
#
# Or manually:
#   pip install -r requirements.txt
#
# NOTE: oauth2client must NOT be installed.
#   It causes "invalid_grant: Invalid JWT Signature" with modern GCP keys.
#   Run:  pip uninstall -y oauth2client
# =============================================================================


# -----------------------------------------------------------------------------
# PyTorch  (CPU + MPS for Apple Silicon)
# Standard PyPI wheels include both CPU and Apple Silicon MPS support.
# No special --index-url needed since PyTorch 2.0.
# -----------------------------------------------------------------------------
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0


# -----------------------------------------------------------------------------
# Core scientific / data stack
# -----------------------------------------------------------------------------
numpy>=1.24.0,<3.0
pandas>=2.0.0,<3.0
scipy>=1.10.0
scikit-learn>=1.3.0,<2.0
statsmodels>=0.14.0
joblib>=1.3.0

# torch is version-pinned in a range so it resolves against whichever index
# the caller configures: pytorch.org's /whl/cpu for x86_64 Linux (CI + Docker),
# default PyPI for macOS ARM / CUDA hosts. The Dockerfile + GH Actions
# workflows pass `--extra-index-url https://download.pytorch.org/whl/cpu` to
# pull the CPU wheel; everyone else gets the default torch build.
torch>=2.6,<3.0

yfinance>=0.2.38
plotly>=5.20

# P1 data ingestion + sentiment
pyarrow>=14.0
snscrape>=0.7
beautifulsoup4>=4.12
# Article body extraction for news_ingest. Purpose-built for arbitrary news
# sites; falls back across multiple extraction strategies.
trafilatura>=1.12
transformers>=4.36
sentencepiece>=0.1.99
google-cloud-firestore>=2.16


# -----------------------------------------------------------------------------
# Finance / time-series data
# -----------------------------------------------------------------------------
yfinance>=0.2.40
pandas-ta>=0.3.14b0


# -----------------------------------------------------------------------------
# Excel / spreadsheet I/O
# -----------------------------------------------------------------------------
openpyxl>=3.1.0


# -----------------------------------------------------------------------------
# Google Sheets / Cloud auth
# -----------------------------------------------------------------------------
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
gspread>=6.0.0
cryptography>=42.0.0


# -----------------------------------------------------------------------------
# HTTP / networking
# -----------------------------------------------------------------------------
requests>=2.31.0
urllib3>=2.0.0
httpx>=0.27.0


# -----------------------------------------------------------------------------
# Web / API server
# -----------------------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0,<3.0
python-dotenv>=1.0.0


# -----------------------------------------------------------------------------
# LangChain (routing / orchestration)
# -----------------------------------------------------------------------------
langchain-core>=0.1.0


# -----------------------------------------------------------------------------
# Visualisation
# -----------------------------------------------------------------------------
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.18.0


# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
tqdm>=4.66.0
beautifulsoup4>=4.12.0


# -----------------------------------------------------------------------------
# Hyperparameter optimisation
# -----------------------------------------------------------------------------
optuna>=3.5.0


# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest>=8.0.0
